#include <stdio.h>

int main() {
    int length, width;
    printf("Enter the length of recangle: ");
    scanf("%d",&length);
    printf("Enter the width of recangle: ");
    scanf("%d",&width);
    printf("The area of rectangle is %d",length*width);
    return 0;
}